{
  "pluginAlias": "PuraSmartDiffuser",
  "pluginType": "platform",
  "singular": true,
  "x-branding": {
    "imagePath": "branding/banner.png",
    "iconPath": "branding/icon.png",
    "image": "https://raw.githubusercontent.com/homebridge-plugins/homebridge-pura/latest/branding/banner.png",
    "icon": "https://raw.githubusercontent.com/homebridge-plugins/homebridge-pura/latest/branding/icon.png"
  },
  "x-icon": "branding/icon.png",
  "icon": "https://raw.githubusercontent.com/homebridge-plugins/homebridge-pura/latest/branding/icon.png",
  "headerDisplay": "<img src=\"https://raw.githubusercontent.com/homebridge-plugins/homebridge-pura/latest/branding/banner.png\" alt=\"Pura Smart Diffuser\" width=\"180\" style=\"height:auto;\" />",
  "strictValidation": false,
  "schema": {
    "type": "object",
    "required": [
      "name",
      "username",
      "password"
    ],
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Pura Smart Diffuser"
      },
      "username": {
        "title": "Username",
        "type": "string",
        "description": "Your Pura app username (email address)"
      },
      "password": {
        "title": "Password",
        "type": "string",
        "description": "Your Pura app password"
      },
      "forceNightlightOff": {
        "title": "Force Nightlight Off",
        "type": "boolean",
        "default": false,
        "description": "Pura turns the nightlight on/off with the diffuser. If enabled, the plugin will send a nightlight off command right after turning on a diffuser."
      },
      "enableFanService": {
        "title": "Enable Intensity Control",
        "type": "boolean",
        "default": false,
        "description": "Replaces the on/off switch with a fan accessory to control intensity (Subtle, Medium, Strong). For multi-bay diffusers, HomeKit intensity changes are applied across available bays to keep auto-alternate behavior consistent. Changing this may require recreating HomeKit scenes or automations for all Pura diffusers in this plugin."
      },
      "enableBayControl": {
        "title": "Enable Per-Bay Controls",
        "type": "boolean",
        "default": false,
        "description": "Exposes separate controls per bay (Switches, or Fan controls when Intensity Control is enabled). Only one bay can run at a time; turning one bay on will turn the other bay off."
      },
      "enableNightlightAccessory": {
        "title": "Enable Nightlight Controls",
        "type": "boolean",
        "default": false,
        "description": "Enables nightlight controls (On, Brightness, and Color) on the diffuser accessory (Single Tile) for compatible diffusers."
      },
      "enableAutoAlternate": {
        "title": "Enable Auto-Alternate Control",
        "type": "boolean",
        "default": false,
        "description": "Adds a switch for Pura's Auto-alternate fragrances setting on multi-bay diffusers, so it can be changed from HomeKit rather than the Pura app. When it is on the diffuser runs both bays, alternating between them; when off it runs one bay at a time."
      }
    }
  },
  "customUi": true
}
